services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
      args:
        FLUTTER_VERSION: "3.41.9"
    image: mpibrasil-flutter:2.3.4
    ports:
      - "8080:80"
    restart: unless-stopped
